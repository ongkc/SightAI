# Getting Started with Create React App

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## Command to start the Application

In the project directory, you can run:
```
Step 1: cd into front-end/my-app
Step 2: run docker-compose build
Step 3: run docker-compose up
```
## Application Manual and Guide
```
1) View the application on http://localhost:3000/ and you will be redirected to Login page
2) Login with username: sa-user, password: UYbLmpxq8aQ9, 
3) With successful login, user will be redirected to signature generator page and a session of 1 hour will be created before the login token expire.
4) At the signature generator page, the user can upload image file in JPG, JPEG or PGN format to generate a signature.
5) With successful image upload and signature generated, the signature will be displayed below the submit buttom.
4) Once signature generator token has expired, the user will be redirected to login page to login again. 
```